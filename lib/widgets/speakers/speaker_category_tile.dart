import 'package:flutter/material.dart';

class SpeakerCategoryTile extends StatelessWidget {
  const SpeakerCategoryTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}